local PluginsConfig = {
    list = {
        "rvm-git",
        "rvm-treesitter",
        "rvm-telescope",
        "rvm-which-key",
        "rvm-flutter-tools",
        "rvm-auto-pairs"
    }
}

return PluginsConfig
