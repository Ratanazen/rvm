local LSPConfig = {
    enabled = true,
    auto_diagnostics = true,
    format_on_save = true,
    completion = {
        enabled = true,
        auto_popup = true,
        min_chars = 2,
    },
    hover = {
        enabled = true,
    },
}

return LSPConfig
