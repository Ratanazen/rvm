local EditorConfig = {
    line_numbers = true,
    relative_numbers = false,
    cursor_line = true,
    tab_width = 4,
    auto_indent = true,
    word_wrap = false,
    mouse = true,
    scroll_offset = 3,
    show_gutter = true,
    highlight_matching_brackets = true,
    format_on_save = true,
    current_style = "RVM Classic",
    icons = {
        mode = "auto", -- "auto" | "image" | "glyph"
    },
}

return EditorConfig
